(async () => {
    const prompt = require('prompt-sync')();
    const crypto = require('crypto')

    // Initialize
    const {
        User,
    } = require('./model/init')


    process.exit(0)
})()